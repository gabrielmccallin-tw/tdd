{
  "name": "projects",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "jest --watch"
  },
  "dependencies": {},
  "devDependencies": {
    "@azure/functions": "^1.2.2",
    "@types/jest": "^26.0.14",
    "jest": "^26.5.3",
    "ts-jest": "^26.4.1",
    "typescript": "^4.0.3"
  },
  "jest": {
    "preset": "ts-jest"
  }
}
